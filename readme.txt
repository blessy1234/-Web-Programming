html:
create a main html page and link other page in it.
git:
install the git
config it
clone the remote repository to local repository
intialize git parameters
add files to stagging area(git add .)
create a commit/version(git commit -m firstcommit)
pushed is files to remote repository
create a file readme.txt in the remote repository
pull this changes to local repository
